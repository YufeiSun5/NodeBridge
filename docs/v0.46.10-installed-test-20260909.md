# v0.46.10 实装与宽表测试

更新时间：2026-09-09 20:12 Asia/Singapore。身份：test-ai。

## 当前结论

- 双端正式安装、配置保留、标准索引迁移、MCP 诊断和前置功能已通过。
- 四路 Apply 的短测 `widesmoke_0909_192354` 在中心恢复期限门禁失败，未满 30 分钟。已恢复现场及中心 `sync.apply_lanes=1`，未启动新的七小时测试。
- 旧失败保留，不用隔离基准、恢复探针或早期快照替代完整负载门禁。

## 安装与迁移

安装包：`build/NodeBridge-beta-v0.46.10-20260909.exe`，339521094 bytes。

- Package SHA256: `FF17F3DC28A409D028FA044C16D4F9FFC358BB9EF3162D5ADB79C60412DAA915`
- Agent SHA256: `0A0E3175F40CC15648D42A9B5C265E7D3524DFDAF5E8D51269A47E8F7DE8FEF3`
- 安装包未签名，未提交或对外发布。

中心和 `192.168.10.105` 边缘机均在正式安装目录 `C:\Program Files\NodeBridge\app` 验证版本、运行文件 hash 与 32 位 NSIS 登记一致。安装摘要均 passed；解密后的配置语义与规则保留。安装证据位于 `.cache/v04610-installed-20260909-1836/`，私有配置备份不作为可分发附件。

本版本新增 `sync_apply_log(table_name,op_type)` 非唯一覆盖索引，并由双角色标准 `migrate` 验证/创建；不改变同步写入、幂等、ACK 或分发语义。安装包内 CLI 在独立工作目录验证双角色迁移。

对同一保留失败数据的三次只读测量，迁移后 Apply 聚合中心为 64.463/66.016/95.703ms，Edge 为 231.974/265.744/258.498ms，迁移前后分组计数完全一致。这是停载诊断结果，不是长期吞吐保证。

隔离 80,000 行 Apply 基准为 109.119 -> 37.820ms，行数不变；事件失败计数覆盖索引测试中，未覆盖基线达到诊断期限，覆盖后为 2.149ms。保留基线超时，不将其描述为成功查询。

## 已保留的失败

| Run | 结果 | 证据与处置 |
| --- | --- | --- |
| `widesmoke_0909_180024` / v0.46.9 | metrics 读取超时，未满 30 分钟 | 已恢复；3,189 条精确归属测试消息经 publisher confirm 隔离。Apply 聚合全表扫描是诊断发现，不宣称原始超时唯一原因已证明。 |
| `widesmoke_0909_184033` / v0.46.10，一路 Apply | 18:58:47 生命周期总期限 180 秒超时；每端 39,000 INSERT、19,710 UPDATE | 18:59:03 恢复通过；16 组 Apply 统计无超时，但恢复后端到端延迟达到 21-30 秒。499 条遗留消息精确归属后 confirm 隔离。 |
| `widesmoke_0909_191842` / 四路 Apply | 启动预检发现 499 条旧下行积压 | 未产生负载；不计为功能通过或性能失败。 |

旧 642 条及以上隔离队列保留。未删除业务记录、修改失败状态或追认历史 run 通过。

## 测试夹具修正

FB-071 的范围仅为测试诊断与准入：

- 生命周期记录逐端、逐主键、逐阶段 COMMIT/等待 trace，等待错误保留最后比较差异。
- 故障恢复同时检查重启水位和当前双向 INSERT 积压，要求不超过 80 行并连续两次快照满足；恢复总期限不变，生命周期不能抢先开始。
- 短测准入绑定双端原配置、原规则四个 hash，正式 Start/Run 在变更现场前检查，禁止以四路配置通过后换一路配置启动长测。
- 原稳态 P95 <= 2s、全样本 P99 <= 5s、样本下限、SQL/快照期限、负载和全列核验均保留。

隔离跨机 RabbitMQ 测试 500 条 1KiB 持久化消息，确认发布并手动 ACK：basic.get 448.67 条/秒，Consume 9215.93 条/秒。前者高于本轮固定负载，不能据此认定轮询是端到端瓶颈；未修改产品消费者。

FB-072：MCP 概览曾报下行深度 0，但直接 broker 为 499。当前所有空队列门禁以直接 RabbitMQ 查询为准，该产品统计差异仍待 backend-ai 处理。

## 当前短测

`widesmoke_0909_192354` 于 19:24:39 开始，19:41:05 因中心积压恢复超时失败，19:41:20 自动恢复通过。每端 36,900 INSERT、18,450 UPDATE；没有完成全列/oracle/账本及最终延迟门禁。

中心仅 `sync.apply_lanes` 从 1 改为 4 作对比，原配置已备份，解密后其余字段一致；失败后通过安装版 MCP 恢复 1 并重启。前置 50 列 bootstrap、重复事件、所有权交接及六次双向生命周期通过。

Edge 停机 69.325 秒，重启约 83 秒后两次当前积压为 0；Server 停机 66.568 秒，截止仍有 Edge -> Server 560 行、Server -> Edge 1,640 行积压。严格恢复门禁正确拦截，不把重启时水位追平误报成实时恢复。

规则恢复后发现 ingress 140、downlink 360 条遗留，停止双端 Agent、逐条核对本run归属并 confirm 隔离为 500 条，随后双端重启。旧隔离队列保持原数。

边缘临时防休眠租约 PID 13880 于 19:41:22 因规则恢复自动 released，不改电源方案。

后续只读诊断发现中心 MySQL buffer pool 为 128 MiB，累计 `Innodb_buffer_pool_wait_free=196983`。这是全实例历史值，不证明本次降速因果；仍需增量或隔离对照。未修改 MySQL 参数或降低刷盘可靠性。

## 验证与边界

### 原一路完整尝试与现场归因（20:50）

`widesmoke_0909_202640`于20:27:17.817开始计划1800秒，20:43:38.340因中心实时恢复期限失败，上传/下行差值1280/1125。每端36,660 INSERT、18,330 UPDATE；前置50列、重复事件、所有权交接和6次完整CRUD生命周期通过，但未执行最终全部字段/oracle/账本、DDL及延迟汇总，不能准入七小时。

Edge停机68.744秒，20:37:04重启，20:38:27两次实时水位达标（约82.7秒）。中心停机71.616秒，20:41:22重启，直到原恢复期限仍未追平。

| 中心采样时间 | 空闲页 | 脏页 | 累计wait_free |
| --- | ---: | ---: | ---: |
| 20:39:31 | 748 | 3194 | 204295 |
| 20:40:33（停机） | 876 | 176 | 204295 |
| 20:41:37（恢复） | 45 | 5245 | 204295 |
| 20:42:39（恢复） | 0 | 6476 | 205999 |

这是本次负载时间段内的增量1704，不再只是全实例历史等待值。参数仍为128MiB、1个buffer pool实例、128MiB chunk，`innodb_io_capacity=200/max=2000`，`innodb_flush_log_at_trx_commit=1`与`sync_binlog=1`未改。20:46主机可用内存约3.6GiB，仅为实验容量参考，不代表已批准调参。

只读测试观察器每200ms采样当前SQL事件ID、语句种类和表类别，不记录SQL文本/参数，单次2秒期限。20:41:22至20:43:38共680次采样，其中613次有活动语句；事件日志执行出现622次、Apply日志执行169次、COMMIT137次（不同连接可同帧出现，不能简单当CPU百分比）。同一Apply语句ID跨5帧，持续至少800ms。观察查询平均1.115ms、最大9.04ms。等待/stage消费者原本关闭，未打开，不能由空字段推断无IO等待。首版不能分类预处理SQL，已保留为`mysql-statements-v1.jsonl`，第二版用现有进程信息在SQL端归类；两版均未改负载/产品。

读取既有Canal 11112指标，不新增监听或修改配置。恢复中20:42:51 put/get/ack延迟21439/30439/31439ms，produce_mem-consume_mem约16MiB，接近配置容量；20:43:50 ack延迟仍28916ms。结合下行broker多数为空，指向中心数据库/CDC消费瓶颈。原始`mysql-metrics.jsonl`、`mysql-statements.jsonl`、`canal-metrics.jsonl`均在本run目录。缓存压力是强相关证据；还需要同负载128→512MiB可回滚对照，不宣称唯一根因已证明，也不通过降低可靠性或验收标准取得通过。

20:43:52自动恢复passed，20:43:55临时唤醒released。后续停止双端，通过本run原helper逐条检查归属、publisher confirm后ACK隔离1570条，双端正式安装版重新启动。20:49配置与规则hash均等于本run原备份，Agent hash仍为本报告v0.46.10，正常队列ready/unacked全0，旧隔离642/3189/499/500不变。观察器与Canal采样均已退出，无新长测。

收尾全量测试曾因`TestMetricsRowsTrace/success`共用50ms期限而失败。仅调整测试：非超时用例5秒，timeout仍50ms并显式核验DeadlineExceeded，不把正常逻辑测试当性能基准。重复30次及最终全量test/vet/lint通过，产品14秒SQL/15秒外层采样及恢复期限均未改。下一步等待用户确认临时内存对照，未执行SET GLOBAL或修改MySQL配置文件。

### 分发与位点诊断（20:20）

隔离50万历史日志、1000条50列UPDATE：分发2.740秒，364.91/s；两次日志写入合计2.259秒，16次发布合计289.198ms。交错40 INSERT/20 UPDATE：4.057秒，246.50/s，日志3.117秒、33次发布722.752ms。两轮均核对1000条SUCCESS日志、消息顺序和唯一事件；交错另核验类型/表/全部50列。仅新增测试夹具选项`NODEBRIDGE_DISPATCH_MIXED=1`。测试库与队列正常清理，未修改产品。首次调用误把vhost写为不带前导斜线而403，随后使用`%2Fnodebridge-server`成功；首次不属于产品性能失败。

FB-073空闲反馈：中心`sync_event_log`/`sync_apply_log`的performance_schema表写计数在20:18:15与20:19:02均为811989/151958；`sync_upload_offset`从1492829升至1493645。此计数包括INSERT/UPDATE调用，不等于816次事务。位点从`binlog.000262:800410660`推进至`:800585284`。`SHOW BINLOG EVENTS ... FROM 800585284 LIMIT 14`确认紧随其后的连续事务只有`scada_center.sync_upload_offset`的Table_map/Update_rows及BEGIN/Xid。

代码对应`Adapter.Commit`始终在ACK后Save位点，`ServerCanalDispatchRuntime.RunOnce`对suppressed/empty批次也Commit；广域订阅捕获位点自己的更新，形成循环。需要独立修复并验证有效业务位点及ACK不退化；当前未修，不把该空闲开销直接认定为30分钟恢复失败原因。20:18全量`go test ./...`、`go vet ./...`及lint通过（0 issues）。

### 后续诊断短测（不作正式准入）

恢复原一路配置后，`widesmoke_0909_195000` 于19:50:43开始并于20:00:44完成，600秒诊断门禁和恢复均 passed。每端21,420 INSERT、13,290 UPDATE；50列、完整账本/oracle、4次生命周期、两次故障、DDL、晚期精度/重复/交接和3次队列静默通过。

每方向42组延迟（稳态31、压力11）；Edge/Server稳态P95为403.789/625.108ms，全样本P99为409.100/2249.718ms。Edge实际停机31.349秒、约40.5秒确认恢复；Server停机25.432秒、约43.1秒确认恢复。较短故障/负载窗口通过不推翻1800秒失败，不可签发正式准入。

550.713秒的MySQL采样区间内，中心buffer pool物理读增量69,819、fsync增量145,142；Edge分别436、102,974。双端`Innodb_buffer_pool_wait_free`增量均0，尚无缓存等待导致本次失败的证据。MySQL配置不变。

前一诊断启动`194756`因监督器仅匹配反斜线配置路径而误报Agent未运行，未产生负载；使用规范路径重新启动后通过预检。其防休眠请求拒绝非活动规则，未修改电源设置。`195000`租约20:00:41随规则恢复释放。正常broker队列均0，历史隔离642/3189/499/500条保留。

隔离连接基准（空库，50列、800 INSERT/400 UPDATE、所有版本与1,400条Apply账本精确核对）默认驱动约490-512事件/秒，驱动内置参数插值约627-652事件/秒。这是分段空库基准，不是端到端数据；未因此修改产品DSN或同步逻辑。大历史日志对照另测。

50万条隔离历史日志、随机分布事件ID对照随后通过：默认模式502.492/508.250事件/秒，参数插值664.457/585.858事件/秒；四次各800 INSERT、200条最终version=3、501,400条Apply日志精确一致，测试数据库均清理。耗时143.65秒，证据`.cache/fb071/connection-performance-history.log`。仍未复现完整链路降速，不把本基准作为修改产品连接设置的充分依据。

20:09:59再次在内存解密对比中心配置，所有字段与调四路前备份完全一致；证据`.cache/fb071/config-restoration-proof.json`。全量Go test/vet/lint在最终测试文件变更后通过，0 issues。

已运行 `go test ./...`、`go vet ./...`、`golangci-lint run ./...`（0 issues）、监督器回归、生命周期 SQL mock 回归、Wails 构建与前端测试、双角色包内迁移、安装包 hash/资产/覆盖升级/32/64 位登记回归。

待当前完整短测通过、自动恢复通过、实测空间预测与配置/hash 准入签发后，才允许新七小时启动。长测启动稳定不等于七小时完成。

### 用户授权的缓存配置复测（231556未通过）

- 用户明确授权调整并保留配置，最终要求中心8GiB、边缘2GiB。23:14分别执行`SET PERSIST innodb_buffer_pool_size=8589934592`和`2147483648`，运行值、持久值、`persisted_globals_load=1`与resize Completed均核验；`innodb_flush_log_at_trx_commit=1`及`sync_binlog=1`不变。中心为Docker MySQL 8.0.38（mysql_data持久卷），边缘为MySQL 8.4.11。
- `231352`在无负载预检发现中心原Agent未运行，未进行业务写入；通过正式安装版MCP启动中心后，新`widesmoke_0909_231556`于23:16:42开始完整1800秒实装负载，预计23:46:42结束。安装路径、0.46.10版本和包内Agent hash仍一致。未修改产品代码、负载或恢复门禁。
- 前置50列、重复/旧UPDATE回放及同键交接通过。Edge计划停机69.168秒，23:27:52连续两次实时恢复采样通过；Server计划停机72.458秒，23:32:15连续两次恢复采样通过，两次最终判定差值均为上传0/下行40条；后续快照双向差值0。恢复后的生命周期检查通过。
- 至23:32:03中心`Innodb_buffer_pool_wait_free`相对本轮首次采样增量0，空闲页481333；本轮已通过此前失败的中心恢复阶段，但DDL、峰值、最终全列/oracle/账本/延迟/恢复及完整时长仍需核验，不能提前签发准入。
- 两端同时改变缓存，不是中心单变量因果实验；这些证据不能证明FB-071唯一根因，也不关闭独立的FB-072/073。正式七小时尚未开始。全量Go test/vet/lint在23:18前通过。

23:39:05本轮因“当文件已存在时，无法创建该文件”终止，最终每端52560 INSERT/30470 UPDATE。此前DDL add/drop及晚期probe/handoff通过，但未完成30分钟和最终门禁，不签发准入。自动恢复passed，正常Agent69948/15576及两端原配置/规则hash实测一致，awake65968已released；76条本run消息逐条确认归属并publish-confirm隔离，正常broker队列0。

FB-074在受控FileShare.ReadWrite读锁下，用旧Move-Item状态写函数复现相同错误（MoveFileInfoItemIOError）。原始失败只保留消息，无法追溯当次准确调用行；监督器新增错误位置/调用栈记录，并用File.Replace原子替换与2秒有界共享锁重试，持续锁仍失败且保留旧状态。专项覆盖首次创建、覆盖、瞬时锁、持续锁、旧值完整、临时文件清理、UTF8及允许删除共享读取；专项与原监督器、全量Go test/vet/lint均通过。只修改测试脚本，不改同步行为，不放宽业务期限；须从零再跑完整1800秒。

### 234447最终验收失败（2026-09-10 00:22）

本轮于23:45:41开始，00:13:31两端均完成1671个生产tick、64220 INSERT和39850 UPDATE。12次生命周期、两端计划停机恢复、DDL add/drop、晚期probe/handoff通过。Edge停机68.447秒、23:56:49恢复门禁通过；Server停机71.774秒、00:01:14恢复门禁通过，均连续两次实时差值0/40合格。文件原子替换缺陷未再现。

最终verify在精确Apply日志核对失败：`nb7_widesmoke_0909_234447_s_state/UPDATE expected=39888 actual=39889`。没有生成最终consistency.json或准入证明。重试条件的DateTime与DateTimeOffset混用于00:14:02再次抛错；新增调用栈明确定位监督器条件行。已统一UTC DateTimeOffset比较并补成功/失败/期限边界/跨午夜/默认时钟回归，原始verify错误另行记录；不改变原期限与账本要求。

额外事件`cdc4593773b4f60a36533fa02fe86fba`（中心表setting_id=591、version=4）在23:48:55.894作为server-001来源记录，但after.updated_by_node=edge-001、after.last_event_id=`cdc07f1e8ae43a373144f4dd03e4afd8`。后者是中心sync_apply_log中的真实edge-001 UPDATE，applied_at为23:48:55.793；这些时间是记录字段，不能当成实际事务提交完成时刻。同表同主键/版本没有重复中心来源事件，按after.updated_by_node聚合为server-001 39888条、edge-001 1条，确认为回放泄漏证据（FB-076），不接受计数容差。

Store.Exists使用非锁定COUNT且将查询错误折算为false。新`TestReplayLogReadVisibilityExperiment`在独立数据库中观察performance_schema真实锁等待：未提交记录的普通读取返回0，事务内FOR SHARE等待后在提交分支返回1、回滚分支返回0，0.44秒通过并自动清理。这支持可见性竞态解释，但没有当次SQL调用跟踪，不能宣称唯一原因已直接证明。[MySQL非锁定与锁定读取说明](https://dev.mysql.com/doc/refman/8.4/en/innodb-locking-reads.html)、[binlog事务写入说明](https://dev.mysql.com/doc/refman/8.0/en/binary-log.html)。核心回放错误传播和可见性修复尚待backend-ai实施。

后置独立运行原延迟计算函数，每方向131样本，稳态105/压力26。Edge稳态P95=404.984ms、全部P99=3826.417ms通过（压力最大5438.334ms仍保留）；Server稳态P95=821.292ms，但全部P99=8976.930ms超过5000ms，另列FB-077。自23:45:38至00:13:27两端wait_free增量0，不能用缓存已扩容推导性能门禁通过。

00:14:14自动恢复passed，00:21实测两端配置/规则hash与备份相同，正常安装Agent为Server74960/Edge6832，awake33392已随规则恢复释放，正常broker队列0、历史隔离消息保留。中心8GiB/边缘2GiB持久配置保留。专项、原监督器及全量go test/vet/golangci-lint通过，但本轮业务门禁失败、未满完整30分钟，不签发准入，不启动七小时。
