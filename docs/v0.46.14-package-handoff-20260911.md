# NodeBridge 0.46.14 交付记录

日期：2026-09-11。身份：backend-ai。看板：FB-097 closed。

## 交付物

- 安装包：`build/NodeBridge-beta-v0.46.14-20260911.exe`，339728542 bytes，Windows x64，未签名。
- 安装包 SHA256：`C580851F014FA3672DDD12C0F3BA1FA58E1AF182075DD3DEDC84E7B0BD79BA2D`。
- 包内 SyncAgent SHA256：`368D393269E7493398A372445C555F61234180B03F6E17A7EC8B9FECD776CB22`。
- 包内 UI SHA256：`FCADEF9B16D6210E5FEC1E59A5642AA1186AD3A2C8431E31471E4B52C8EF7285`。
- 业务 AI 说明：[mcp-business-ai-handoff.md](mcp-business-ai-handoff.md)，同文件已包含在安装包 docs 中。
- 生成的客户端配置：`build/NodeBridge-mcp-client-v0.46.14.json`。适用于 AI 在当前中心电脑运行、本机安装默认路径、边缘 `xx@192.168.10.105` 且使用当前客户端密钥路径的场景；普通模式，不默认授权 lab-full-access。它是 CLI 生成结果，不代表已安装两端连通性验收。

两端使用同一安装包，角色来自各自配置。用户自行安装，本轮不部署。中心仍从旧工作区运行 PID15972（交付核验时值）；先从原管理入口停止同步并退出旧 UI，核实工作区 Agent 退出后再安装。不能让新旧副本竞争同一配置。中心现有 Docker 组件按 external 复用，不安装冲突的受管服务。

## 改动

纳入 FB-096 的确认后日志状态 UPDATE 修复，保留原 payload、事务、幂等、ACK/confirm/位点顺序和防回环逻辑。本轮只调整版本、打包文档清单与封包验证，不新增 Wails DTO/UI 或 MCP 方法。

新增 MCP AI 交接文档，说明标准握手、工具/资源发现、本机与 SSH 连接、权限、全规则保存与表列映射约束。分发包不再带 `docs/test-credentials.md`；仍为有默认试用密码约定的内网版本，不作为生产安全承诺。

## 验证

- `go test ./...`、`go vet ./...`、`golangci-lint run ./...` 通过，lint 0 issues。
- Wails production build（含 TypeScript/Vite）、CLI ready/canal-check、Wails contract 通过。
- 本轮版本/打包/看板文件的diff空白检查通过；全工作区 `git diff --check` 因Wails生成的 `frontend/wailsjs/go/models.ts` 空白格式未通过，未为此改动生成器或无关前端代码，不将该项计为通过。
- 42个 staged 文件与安装包解包文件逐一SHA256一致，5项离线资产版本与哈希正确；headless仅预检，未安装现场组件。
- 真实隔离NSIS双次覆盖安装通过：旧测试Agent停止、UI/Agent/schema覆盖、配置和规则保留。证据 `.cache/nsis-upgrade/20260911-100957-409/upgrade-evidence.json`；UPGRADE_TEST变体不安装实际组件或恢复现场UI。
- 32位与64位Windows PowerShell安装回归各15项通过，UI恢复9项模拟回归通过。
- 包内MCP握手0.46.14、initialized通知、ping、33工具及其inputSchema、5资源发现与diagnostic资源读取通过，overview版本一致、真实MySQL只读诊断4步ok。
- 真实隔离启动失败ERROR已写盘；MCP工具和资源读取、数字密码不破坏日志JSON回归通过，绑定同一Agent哈希。
- 总证据 `.cache/v04614-release/package-gates.json`；MCP `.cache/v04614-release/package-mcp-version.json`；日志 `.cache/v04614-release/runtime-logs/evidence.json`。旧版本证据未覆盖。

## 未完成

FB-094：修复版双端安装后的完整长时压力与125秒延迟故障复验仍open，不能把本轮封包或此前1.55秒SQL锁对照记为七小时通过。FB-084：真实升级后UI恢复待现场验收。FB-089：MCP started_at偶合秘密数字时误脱敏仍open，已在业务AI说明披露。其余旧看板项状态不变。

本轮未停现有业务、未修改两端配置/规则、未执行数据治理或系统迁移、未连接边缘部署、未启动新的长测。安装包未签名，用户应先核对本记录哈希。
