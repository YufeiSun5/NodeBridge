# v0.46.8 安装包与宽表测试准入

Owner: test-ai。最新状态：正式七小时于16:34:10因snapshot采样15秒超时失败，16:34:26自动恢复结束；当前没有长测运行，重跑准入已撤销。此前十分钟通过和启动证据仍有效但不代表长测通过。详见`docs/v0.46.8-seven-hour-interruption-20260909.md`。

## 修复范围

- FB-057：MCP、UI overview 共用 buildinfo.Version，统一为 0.46.8。
- FB-058：SyncEvent 与 ChangeEvent JSON 使用无损数字解码；Canal 字符串版本正确标准化，避免 BIGINT/DECIMAL 经 float64 丢失精度。
- FB-059/061：Apply 的业务写入和当前回放标记同事务提交，兼容列映射；Edge Canal 与单次上传正确注入 apply-log 查询，构造器拒绝缺失依赖。
- FB-062：连续不同目标主键 UPDATE 合并最多64键的发布确认批次，同键/INSERT/DELETE/DDL仍为确认屏障；失败回退与选择性NACK保持。
- FB-063：监督器修复 PowerShell 变量大小写冲突、UTF8 JSON读取以及清理异常跳过现场恢复；JSON读取允许共享删除，生产器对Windows短暂锁最多重试两秒，持续锁定仍失败并保留旧进度。增加隔离和真实Windows锁回归，主负载速率与目标数量不变。
- FB-064：原方案稳态2秒目标不应与峰值混算。以源COMMIT时刻和预先固定阶段分组，压力原样保留；稳态P95<=2秒，全部样本P99<=5秒。短测采样5秒、正式30秒，至少40总/25稳态/3压力样本；不得按测量结果筛选。
- FB-065：边缘交流电自动睡眠15分钟；使用临时SYSTEM_REQUIRED进程保持自动唤醒，仅本run规则精确匹配时生效，恢复规则后退出，最长25800秒。真实20秒到期和本轮规则恢复释放均通过；未修改电源方案，不阻止手动睡眠/关机。

## 最终包

- 文件：`build/NodeBridge-beta-v0.46.8-20260909.exe`。
- 大小：339,504,861 bytes。
- SHA256：`A24947CC72DC1C649CD3B7BA8F050997B116B386F546D83639A2515B9CEBB59A`。
- 包内 SyncAgent SHA256：`F56CA1D5686D3DF32EF54FE8144331E9617D7C967EA70426AF4AAED20002E5D6`。
- 未签名、未提交、未推送、未发布。此前同名候选包已由本哈希替代，不用旧候选哈希验收。
- 两端短测只临时运行包内Agent；边缘用户安装目录不覆盖，结束恢复原路径/配置/规则。

## 已完成门禁

- `go test ./... -count=1`、`go vet ./...`、`golangci-lint run ./...`（0 issues）。
- Wails production build、package smoke、前端 `npm test` binding contract。
- `scripts/test-wide-supervisor.ps1`：正式/短测精确计划数、参数引用、UTF8无BOM中文、大小写变量防冲突、清理失败仍恢复两端并写失败证据。
- 真实解包41文件，其中39个staging文件逐一hash匹配；5个离线组件资产hash匹配；headless预检版本匹配。
- 包内MCP initialize/overview均为0.46.8，32工具。
- 隔离NSIS两次覆盖：运行Agent停止、二进制更新、原配置/规则保留。
- Windows PowerShell 32/64位各15项安装器回归。

证据：`.cache/wide-seven-hour/package-gates.json`、`package-mcp-version.json`；NSIS：`.cache/nsis-upgrade/20260909-153821-736/upgrade-evidence.json`；64位：`.cache/installer-regression/c0c743b6c0774d10ba26a2e608183430/regression-summary.json`；32位：`.cache/installer-regression/083544f9c4604158abf51375a10196c4/regression-summary.json`。

## 真实短测

最新run：`widesmoke_0909_160102`，**completed**。2026-09-09 16:01:44.696至16:11:45.190（Asia/Singapore），完整600.494秒。

- 每端精确提交21420 INSERT、13290真实UPDATE，557个生产tick；额外探针/DDL写入单独记账。
- 六张主表50列逐列比较、完整事务oracle、apply/SUCCESS事件账本、字段修改覆盖及NULL往返均通过；11176次在线版本观察无回退。
- 两端停机恢复、5个CRUD生命周期、双向ADD/DROP、首/晚期精度与4+4重复/旧更新重放、两次同键所有权交接均通过。
- 7条运行队列连续3次空；原规则hash、配置和原Agent路径自动恢复通过；临时唤醒在16:11:41因规则恢复释放。
- 每方向43个延迟样本（稳态36、压力7）。Edge -> Server稳态P95=206.636ms，全部P95=403.629ms/P99=808.016ms；Server -> Edge稳态P95=620.148ms，全部P95=1842.609ms/P99=2445.866ms。压力最大值完整保留；即使合并全部样本，P95也低于2秒。
- 准入审查16:12:29通过，实测数据卷变化556.888秒；Edge/Server观测增长率约489037/1717804 bytes/s。`.cache/wide-seven-hour/formal-ready.json`匹配全部运行hash，非七小时通过证明。

失败历史保留：143533暴露Edge日志依赖缺失；144049触发负载场景超时，停载恢复一致性不替代原失败；151711数据门禁通过但恢复脚本失败，人工恢复后仍保持failed；153921在148秒时遇到进度JSON替换锁，每端5060 INSERT/2530 UPDATE后失败，自动恢复passed；154525全部50列/账本、场景及恢复通过，但13个混合延迟样本的Server P95=2458.163ms而failed，超限样本在峰值/更新重载交界（15:53:18），同期两个方向队列有积压，不删除或追认通过。各轮证据在`.cache/wide-seven-hour/`对应run目录，642条历史隔离消息不变。

## 准入与限制

`scripts/approve-wide-test-readiness.ps1`只接受最新完整600秒且恢复成功的短测，核对包、Agent、helper、脚本、schema哈希和全部证据，并用不少于9分钟的全数据卷空闲变化预测七小时空间。正式Start/Run均校验该证明，任何变化须重新验收。

准入不是七小时通过。正式计划每端900,000 INSERT和558,000 UPDATE，390分钟停产，420分钟内排空、全量核验并恢复；故障仅为Agent停机，不声称验证broker自动重连。Race测试仍受本机CGO/C编译器条件限制。

全仓`git diff --check`仅在Wails自动生成的`frontend/wailsjs/go/models.ts`报告尾随空白/文件末空行；排除该生成文件后通过。保留生成器输出，不把非行为格式清理混入本次修复。

## 正式运行

- Run：`wide7h_0909_161239`；开始2026-09-09 16:13:22.046，计划结束23:13:22.046（Asia/Singapore）。
- supervisor PID72756，producer PID52756；Server Agent PID30160，Edge Agent PID13584；临时唤醒PID10724，最长到23:23:36，正常情况下规则恢复后提前退出。
- 证据目录：`.cache/wide-seven-hour/wide7h_0909_161239/`。`startup-verification.json`记录16:13:40与16:14:59两次相隔78秒的快照：Edge提交INSERT/UPDATE由240/120至1820/910，Server由260/130至1840/920，两端目标由260/240分别增至1820，失败事件/ACK均为0，Agent PID稳定。`awake-lease.json`保留唤醒启动记录；最终结论仍以完整state、全量验证及restoration为准。
- 结束本对话不会停止隐藏后台进程。保持机器供电、网络和共享MySQL/RabbitMQ/Canal可用；不要手动休眠/关机。
- 查看：`powershell -File scripts/lab-real-wide-seven-hour.ps1 -Action Status -RunId wide7h_0909_161239`；受控提前停止用同一命令`-Action Stop`。提前停止不算通过。
