# v0.46.12 Event Log Batching

backend-ai已交接test-ai（FB-082）。FB-081承接FB-080压力延迟失败，未宣称完整短测或长测通过。

## 依据

v0.46.11的`widesmoke_0910_083751`完成每端64220 INSERT/39850 UPDATE、全50列/oracle/精确账本、双故障恢复与12生命周期，但全部样本P99为Edge6852.765ms、Server12428.633ms，超过5000ms。两端wait_free/log_waits增量0，原现场已恢复。

隔离1000个混合宽事件加320个已提交回放记录，原分发4.400秒，事件日志3.097秒、发布确认0.607秒、回放查询0.517秒。日志批接口仍逐条执行预编译SQL，有N次往返。一次100万历史准备阶段被测试DSN的30秒读取超时终止，临时库清除已核验，不能作为产品负载结论。

## 修改

- Store事件日志批写改为同一事务内的多行INSERT/ON DUPLICATE KEY UPDATE。
- 每段最多128行、参数估算上限1MiB；单个超大事件仍单行发送，由原驱动/服务器限制判定，不静默丢弃或截断。
- 不重排事件；保留重复ID的顺序及既有更新字段、payload/SkipPayload、时间语义。
- 任意分段SQL、编码、校验或提交错误均返回；后段失败回滚整个事务。发布确认、业务提交后ACK及回放锁定读不变。
- 无SyncEvent/Wails API/DTO/UI/配置项变化。

## 验证

- 单测覆盖0/1/128/129/257行、字节分段、单个超大payload、开始/首段/后段/提交失败、后段校验和编码失败、取消、payload及SQL参数数量。
- 真实MySQL使用独立数据库逐条写入作对照，257个最终事件全部16字段一致，含段内/跨段重复ID、特殊字符、大payload与SkipPayload；第二分段严格SQL失败后首段记录数为0。数据库自动清理。
- 同一隔离基准修改后2.244秒，日志1.148秒，发布0.395秒，回放0.519秒；这是局部优化证据，不证明唯一根因或压力延迟通过。
- 版本更新前全量`go test ./...`、`go vet ./...`及`golangci-lint run ./...`通过。0.46.12打包、边缘安装、中心工作区运行和完整1800秒准入待验证。

## 最终候选包

- 最终0.46.12版本全量test/vet/lint通过，Wails/Agent构建与离线预检通过。
- `build/NodeBridge-beta-v0.46.12-20260910.exe`，339521250字节，SHA256 `F3A9E39834F8D7A537A5BAC0C4FAECB484EA85046200FEB665050E6474CD0952`。
- Agent SHA256 `E61F24D8DA79504268928F272EC0F56694060905BDA6FC40F6A3145C13F3B003`。
- 41解包文件与5资产hash、两次隔离覆盖安装、32/64位各15回归、MCP/UI 0.46.12及33工具通过；包未签名/发布。
- 覆盖证据`.cache/nsis-upgrade/20260910-092911-639/upgrade-evidence.json`；回归目录`8d57751362a446debb50923056014534`、`8ab73f52385f4f0fa338b015bd64fada`；完整包门禁`.cache/wide-seven-hour/package-gates.json`。
- 实际现场升级及完整短测仍待FB-082；中心只切换工作区候选，不安装、不重试UAC。

## 现场进度

09:33边缘安装完成（PID6348退出0），安装版Agent hash匹配，配置解密全文及规则保留；MCP 33工具/0.46.12/start/诊断通过，MySQL仍2GiB。中心39956优雅退出，工作区新候选42184启动并核验原配置/规则未变，MySQL仍8GiB。备份及边缘安装摘要在`.cache/v04612-installed-20260910`。

`widesmoke_0910_093438`前置50列、重复旧UPDATE和同键交接通过，runner54528，09:35:29开始完整1800秒，预计10:05:29结束。边缘临时防休眠4940已确认active，最长10:11:14或恢复原规则时提前释放。完整门禁未通过前不启动七小时；FB-080旧延迟失败保留，不追认。

## 完整准入结果

093438于10:05:30完成，持续1800.925秒，状态completed，恢复passed；两端原配置/规则/Agent路径恢复，防休眠4940释放并退出。

- 每端完整64220 INSERT/39850真实UPDATE；全部50列、事务oracle、精确Apply/SUCCESS账本通过。
- 33452次在线版本观察、12生命周期、双端故障积压实时恢复、ADD/DROP、初期/晚期精度、重复/旧UPDATE与同键交接通过。
- 每方向135延迟样本，保留102稳态和33压力样本。Edge稳态P95=607.918ms、全部P99=1011.091ms；Server稳态P95=634.690ms、全部P99=2893.418ms。原稳态P95<=2000ms、全部P99<=5000ms门禁均通过。
- 三次队列静默采样及最终恢复通过，未缩减负载、放宽阈值、清除历史数据或改变可靠性参数。
- 证据：`.cache/wide-seven-hour/widesmoke_0910_093438/{state,gates,consistency,event-ledger-consistency,version-observations,restoration}.json`及`awake-restoration.json`。

## 七小时启动

10:06正式准入生成，绑定WorkspaceServer、实际候选路径、二进制/配置/helper/脚本/schema及包hash；磁盘增长实测1674.24秒，空间预测通过。

`wide7h_0910_100650`于10:07:37.956开始25200秒，计划17:07:37.956结束。runner33492、producer29224；10:11实际进程及相隔139.794秒的快照验证通过，双向新增约900→3680，UPDATE和目标落库持续增长，失败ACK/事件为0。证据位于该run的`stable-start.json`。

实际Agent中心69920为工作区候选、Edge7832为安装版，均E61 hash；配置与准入一致。边缘防休眠5388 active，最长17:19:21或规则恢复时释放，不改电源方案。长测自动执行后续阶段及恢复；启动稳定不等于七小时完成，最终结果留FB-083。

当前交接身份test-ai。FB-080/081/082闭环；FB-083长测、FB-072概览统计、FB-073纯空闲位点复核等既有项仍待后续验收。
