# 0.48.1 无业务字段双向回放修复

## 交付

- 安装包：`D:/DEV_D/NodeBridge/build/NodeBridge-beta-v0.48.1-20260914.exe`，340867563字节，未签名。
- 安装包SHA256：`0DBBBDFBC3090C0CDF9BCCAD90B69B4528B99FB4C3249E88B43756DA3A4124D4`。
- 便携Agent及全部迁移：`D:/DEV_D/NodeBridge/build/NodeBridge-agent-v0.48.1-20260914.zip`。
- ZIP SHA256：`2D18B3A0E88350A2BC486A711BB10F83BE5FB1E4AE61518635B6B051F3029D97`。
- Agent SHA256：`13F7B192B7C303EF6DE94D06B232C16CDED8A0DC474876DF4C51B930F7EEB9BE`，与最终实测副本及安装包一致。

## 业务 AI 接续

1. 主站和105均使用0.48.1；更新MCP实际启动程序路径并重开stdio会话，旧进程不会自动变成新版。便携包可放独立目录，无需覆盖业务正在使用的安装目录。
2. 在各自配置所属Windows账户下，先核对config指向NodeBridge系统库，而不是业务库，再执行新版Agent的系统迁移。不要改业务表、不要删除首次对齐作业解除门禁。
3. 主站执行 `SyncAgent.exe migrate -config C:/ProgramData/NodeBridge/config.yaml -scope server`；105执行相同命令但 `-scope edge`。程序同目录须保留完整migrations目录。迁移010新增sync_replay_marker与sync_replay_position，系统账户需相应权限；不修改sys_detection_standards。
4. 保留现有业务规则/连接，使用原MCP首次对齐方法重试。普通无软删除字段表沿用HARD删除，不能用加同步列或切换未授权业务库绕过校验。
5. Canal服务端过滤须包含系统库sync_capture_fence与sync_replay_marker；客户端订阅由Agent补齐并通过完整事务探针验证。缺失标记时不会静默放行写入。

本任务未安装现场程序、未执行现场系统迁移、未继续业务表复制。标准表现场验收由业务AI沿既有授权接续；只读核对确认标准表已有业务updated_by_node，新协议保留它的业务原值。

## 实现及验证

双向LWW不再要求业务表包含last_event_id/updated_by_node，也不覆盖同名业务列。远端写入及冲突修复在同一MySQL事务中插入系统BEGIN/END标记；CDC先持久记录MySQL UUID/binlog文件/位置及目标库表，再过滤回放。屏障只有完整标记/pulse/标记被观察并确认后才放行；不按行值或时间窗口猜测回放。

- `go test ./...`、`go vet ./...`、`golangci-lint run ./...`通过。
- 最终包内Agent：无同步字段双节点、空起点首次对齐、Canal batch=1、CRUD/删除/离线冲突/重启/收据失败重试/repair排空，50.89秒通过，证据`.cache/canal-business/c2d2d271eb114e52ad03f0583d5994d7`。
- 最终包内Agent：三独立MySQL、普通MCP首次全量、多向CRUD/中继/离线更新删除/重启/收据重试/稳定收据数，28.05秒通过，证据`.cache/multi-node-sync/9a200477bce447f39a003e489de88fd1`。
- 前序源端/中心端首次对齐分别54.13/52.60秒通过，前序三节点27.30秒；最终交付哈希以重跑结果为准。
- 封包62文件/20迁移一致，MCP0.48.1/42工具/5资源通过，17安装器回归通过；`.cache/v0481-release/package-gates.json`及`mcp-gate.json`。
- 本轮隔离容器/网络由夹具自动删除，未重启七小时长测。

## 未覆盖

未验证现场标准表完整业务流程、七小时稳定性、200MiB容量及旧协议已有积压原地升级。历史活跃双向链路须先处理旧协议积压，不能直接清空账本；标记/边界在CDC仍可重读期间不得删除。时钟偏差/LWW源时间问题未修复，FB-094及FB-107其他历史项保持open。
