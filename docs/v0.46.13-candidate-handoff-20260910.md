# v0.46.13 候选交付

身份：backend-ai。目的：交付可供用户复测的性能/错误诊断/升级候选，不宣称双端长期高负载已经通过。

## 改动

- Edge批量下行真正进入ApplyBatch；固定utf8mb4驱动参数插值减少往返。配置屏障、DDL、同键顺序、映射、提交后ACK与回放幂等保留。
- 持久化结构化错误与慢步骤诊断，8MiB当前文件＋4份轮转，写入脱敏、Windows共享读、失败回退与恢复记录。
- UI GetLogs保持DTO；MCP nodebridge_logs与nodebridge://logs读取当前及轮转记录，显式-log兼容。JSONL逐字段脱敏，不再通过纯数字密码替换破坏PID/耗时数值。
- 保留NSIS MUI2，增加中文/英文/日文欢迎与完成页、品牌位图及DPI。升级记录原UI会话，用临时Limited InteractiveToken任务恢复；无交互桌面/模糊会话不启动，失败写warning，不改自启动。

## 验证

- Go全量test、vet、golangci-lint（0 issues），Wails生产构建、前端契约测试、CLI smoke通过。
- 真实MySQL＋RabbitMQ，50列1200混合下行：逐事件62–81/s，批量353–410/s；50万历史批量356–401/s。全列、版本、业务行数和Apply总数通过。仅隔离本机基准，不包含远端/Canal/双端同步压力。
- 真实SQL约束失败：业务/Apply均0、三条全部重入队；恢复后提交三条，重复回放不新增，Broker队列0。
- 最终Agent真实缺失broker配置退出1，ERROR/time/node/version/pid/error实际落盘；MCP工具及资源读回，额外把测试密码设为PID验证纯数字脱敏仍保持合法JSON和原数值。证据：`.cache/v04613-final-wire-gate/evidence.json`，Agent哈希与最终包完全一致。
- 32/64位各15项安装回归、9项UI恢复选择器/模拟任务回归通过；中文欢迎/目录页实际窗口检查未见文字重叠。模拟任务测试不是实际桌面恢复验收。
- 最终安装器42个载荷文件逐一解包hash、5个组件资产、两次隔离覆盖安装、32/64位各15回归、MCP33工具/版本/只读诊断全部通过。记录在`.cache/wide-seven-hour/package-gates.json`，覆盖安装证据`.cache/nsis-upgrade/20260910-161450-491/upgrade-evidence.json`。

## 复测边界

- FB-085：双端同等峰值持续运行、故障恢复、精确账本、完整延迟门禁，旧七小时失败及撤销准入保留。
- FB-084：原用户桌面下升级恢复UI的实际验收，尤其SSH发起安装的场景；Session0不直接启动窗口。
- FB-072/073及其他既有open/blocked项仍以AI_BOARD为准。没有部署候选、修改现场MySQL可靠性或清客户历史，没有自动启动新长测。
- 安装包仍未签名；不得把SmartScreen或UAC交互绕过写成验收步骤。

## 包信息

- 路径：`build/NodeBridge-beta-v0.46.13-20260910.exe`
- Agent SHA256：`6C07957C7802F1BCC2EC624FB5E5CE89368CCBC51A513B761DEB5011EB42C30D`
- 安装包SHA256：`5A38F8DA4D04CD7747C2322E73CF5F5F3796026EA72F161D6701ADAB91319DB7`
- 大小：339704992字节（约324MiB），签名状态NotSigned。此前同版本构建的07C4/A246哈希候选因MCP二次脱敏回归被替换，不是交付物。
