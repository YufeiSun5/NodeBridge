# v0.46.8 修复与长测准入方案

Owner: backend-ai -> test-ai。当前状态：正式长测wide7h_0909_161239于16:34:10因snapshot超时失败，16:34:26恢复结束；准入已撤销，当前无长测运行。FB-055 blocked、FB-066 open，详见`docs/v0.46.8-seven-hour-interruption-20260909.md`。下列已完成项仅记录历史短测，不代表当前可重开。

后续v0.46.9候选已完成诊断和包验证，须正式安装后执行至少30分钟准入，详见`docs/v0.46.9-diagnostics-and-upgrade-20260909.md`。

## 历史工作顺序

1. FB-058：SyncEvent 自定义 JSON 解码统一使用 json.Number，不经 float64 转换；覆盖大小整数、精确小数、嵌套数据、错误输入与运行时各消费/重放入口。Canal 字符串 sync_version 必须正常标准化。
2. FB-059：Apply 在已有回放元数据列上写当前 event_id 和 origin_node_id，和业务写入/apply log 同事务；不修改转发的源事件，不假定业务表列同名。单条、批量、compact、严格 append-only、DELETE/再插入和业务主动更新都回归。旧失败数据只保留证据，不用丢事件或 UPDATE 忽略覆盖修复缺陷。
3. FB-057：统一版本来源为 v0.46.8，构建、安装、MCP 与 UI 版本核对；无新的前端接口字段。
4. FB-055：生命周期/DDL/探针独立执行，监督循环继续心跳、故障时钟和采样；完善停止、故障恢复期限、资源预测、延迟/速率、全部50列语义比较与最终队列稳定门禁。正式420分钟包含停产/排空/校验/恢复，不静默提前通过或延长。
5. 单元测试、go vet、golangci-lint、打包/安装夹具先过；生成修订包，临时使用相同包内二进制测试双端，保留原安装与规则备份。
6. 同一修订版本完成隔离边界回归、真实 Canal 边界、至少十分钟宽表 smoke；审查证据后才写匹配 hash 的 formal-ready 记录，启动正式长测并确认后台独立持续推进。

## 交付边界

- 延迟统计修正：按原方案稳态P95<=2秒；固定270-300逻辑分钟为峰值、300-330为更新重载。按每方向源COMMIT时间分类，不根据结果删样本；压力统计完整保存，所有样本P99仍须<=5秒。短测采样间隔5秒（正式30秒），至少40总/25稳态/3压力样本。154525的混合P95失败保持不变，必须新跑完整短测。

- 不改变 ACK 在业务及日志提交后的不变量；不放宽严格主键冲突、不绕过回环和版本回退检查。
- 所有普通业务列保持精确比较；last_event_id 等系统列按回放语义验证，不能简单全部忽略。
- 保留既有642条失败隔离消息，不混入新 run，不 purge 共享队列。
- 不修改用户已有无关代码，不发布、不提交、不推送；自动恢复原配置、规则与原进程路径。
- 用户明确愿意安装更新。后续先定位修复并交付候选包，再验收双端正式安装路径、版本、hash及正常启动入口；临时Agent仅用于诊断，不代替正式安装验收。

## 验收清单

- [x] 数字无损回归与所有事件入口覆盖。
- [x] 回放元数据/同键顺序/批量幂等回归。
- [x] 版本来源及修订安装包验证。
- [x] 测试器异步与超时、恢复、资源和一致性门禁。
- [x] 修订包真实十分钟宽表 smoke。
- [ ] 正式安装与重新准入：原后台启动验证成立，但约21分钟后失败；解决FB-066后须双端正式安装验收及更长历史日志压力准入，再从零重跑。
