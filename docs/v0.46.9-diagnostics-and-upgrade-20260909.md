# v0.46.9 诊断修复与安装交付

Owner: backend-ai -> test-ai。2026-09-09 17:32 Asia/Singapore。FB-067完成，FB-068待实装；FB-066 open、FB-055 blocked。当前无长测运行。

## 已确认与未确认

- 原七小时run `wide7h_0909_161239`约21分钟后因snapshot超过15秒失败，旧失败不改写。原始失败缺逐SQL计时，因此不能声称已证明唯一根因。
- 新增逐连接/逐SQL开始、结束、耗时记录后，对保留现场进行6次只读快照：全部完成，进程总耗时1128-2527ms；没有启动生产器或替换现场Agent。
- 现有中心`sync_event_log`失败统计使用`idx_table_pk(table_name,pk_value)`，需要回表读取`status`。本run的77072条记录实测EXPLAIN ANALYZE约971ms，去除status条件的覆盖计数对照约71ms。后者仅用于诊断对照，不替换正式失败检查。
- 中心buffer pool为128MiB，事件表元数据报告约813MB数据/183MB索引。元数据行数是估计值，不作为精确账本；缓存和写入竞争是否完整解释原始15秒超时，仍需实装负载复验。<!-- 待确认 -->

## 产品修改

- 新增实验室只读MCP工具`nodebridge_mysql_diagnostics`：连接与查询耗时、固定系统表大小/索引、buffer pool、可选表名过滤的事件总数/失败数及非执行式EXPLAIN计划。数据库诊断阶段默认5秒、最大10秒，遵守调用方取消；超时或错误返回partial/error/skipped，不伪造0，不返回业务payload、凭据或原始SQL错误。
- 标准`migrate -scope server`幂等补齐非唯一覆盖索引`sync_event_log.idx_table_status(table_name,status)`。核对完整列、可见性和非唯一性；拒绝同名冲突定义，不删除/替换现有索引。在线DDL不支持或失败时明确报错。
- 安装包新增`app/migrations`；迁移优先使用可执行文件旁的schema，支持脱离开发仓库运行。已有数据库须在正式升级验收时显式迁移，诊断接口不会执行DDL。
- 无同步事件语义、ACK时机、回环判断、Wails binding或UI功能变更。

## 测试器边界

- 保留外层15秒采样失败门禁；内部增加14秒共享连接/SQL期限和即时诊断记录，不是放宽超时。
- 测试前强制验证双端注册安装位置/版本、候选Agent哈希及现有运行路径；临时路径、旧版本、错误哈希和旧进程路径均被单元测试证明拒绝。
- 默认smoke改为1800秒；签发七小时准入必须完成至少30分钟的实装smoke及对应的90%时长磁盘观察。旧600秒结果不能重新签发准入。
- 1800秒计划每端64220 INSERT/39850 UPDATE，正式七小时仍为900000 INSERT/558000 UPDATE；Go与PowerShell纯逻辑校验一致。这里是预期工作量，尚未实跑。
- 50列/oracle、失败事件、失败ACK、回放与版本回退、故障恢复、DDL、队列清空、延迟和恢复检查不取消。当前formal-ready仍失效，实际Start拒绝测试通过。

## 验证证据

- `go test ./... -count=1`、`go vet ./...`、`golangci-lint run ./...`通过，lint为0 issues；`go test -race`仍因现有CGO/编译器环境未运行。
- 隔离MySQL实测：80000条、每条4096字节payload，失败数80保持不变；失败计数从2310.761ms降至0.826ms，计划使用覆盖索引，重复升级通过。这不是长期写入吞吐或原始超时消除的证明。
- 最终包内程序在独立10000条夹具、非开发工作目录执行标准migrate成功；总数和10条失败保持不变，临时数据库已清理。现有`scada_center`未执行索引升级。
- 完整Wails/Agent/NSIS构建、`frontend/npm test`契约检查、PowerShell监督器/安装身份门禁测试通过。
- 最终EXE真实解包，41个staging文件逐个hash匹配，5项组件资产hash匹配。32/64位各15项安装器回归通过。
- 隔离`UPGRADE_TEST`安装器执行两次覆盖：旧进程停止、新二进制及两份schema hash、原配置/规则保留通过。此模式不写真实安装注册表、不安装系统组件，不能替代用户机器上的正式安装验收。
- 最终包内MCP initialize和overview为0.46.9、33个工具；新只读诊断真实调用通过。

证据根：`.cache/wide-seven-hour/fb066-diagnostic-20260909-165019/`，包含6轮采样trace、8万条索引对照、`packaged-migrate.json`和只读MCP报告。最终包证明保留为同目录`package-gates-v0469.json`。隔离覆盖证据：`.cache/nsis-upgrade/20260909-172620-199/upgrade-evidence.json`；位数回归目录为`643915e924f6418dac69e9d55a3802b4`与`c23483311e5a4f519b42ca5e5168e0dc`。

## 安装包与下一步

- 文件：`build/NodeBridge-beta-v0.46.9-20260909.exe`，339524533 bytes，未签名、未提交/发布。
- EXE SHA256：`6C26E6D397C36238A7DEDCBB246D0EC39368F23C40D741F04917B9F6E699514F`。
- Agent SHA256：`9DF8C21E8959E98FE957AF58F31434F6E1BDD847FC6AA7266221795E11C50CDD`。
- 两端需正式覆盖升级，保留原配置/规则及现有MySQL/RabbitMQ/Canal，不卸载或清库。现有Server仍从旧备份路径运行PID17896，Edge仍为原安装路径PID7872；本轮没有永久替换它们。安装后先核验并受控切换旧进程，不允许并行启动两个Agent。
- 实装后明确执行中心标准索引迁移，再用新MCP核查实际索引和查询耗时；完成至少30分钟实装准入后，才能重新从零开始七小时。当前不签发新formal-ready，也不启动长测。
- 旧v0.46.8包和失败run保留；其包证明（含旧MCP证据）已存入`wide7h_0909_161239/package-gates-at-start.json`，旧准入原件在`formal-ready-at-start.json`。
