# v0.46.7 原边缘机安装后连通验收

- Time: 2026-09-09 13:17 Asia/Singapore.
- Identity: `test-ai`; FB-056 closed; FB-057 version-label issue open.
- 结论：**连通通过，20 条真实 Edge -> Server 数据全部到达且内容一致。**

## 安装与运行

用户在原测试电脑 `192.168.10.105`（ADMIN）自行完成安装，最新安装摘要时间为 13:08:34，version `0.46.7`、status `passed`，全部 8 步通过，未跳过系统组件。

- 注册项：32 位视图 `HKLM\Software\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\NodeBridge`，DisplayVersion `0.46.7`。
- 安装路径：`C:\Program Files\NodeBridge`。
- SyncAgent SHA256：`CA4937BE930B371EA7925A256E0BD524756D610BFCA32EA0AF7B7CCE0B0C41B3`，与最终安装包一致。
- NodeBridge SHA256：`15FE051BF98AC7445BEE9F874A55C4978F4E7C8356C9450C8F0182EFCD8859DD`，与最终安装包一致。
- 安装日志：`C:\ProgramData\NodeBridgeInstallerLogs\20260909-130813-009-e60fe234\nsis-beta-install-summary.json`。
- MySQL84、RabbitMQ、NodeBridgeCanal 均 Running。

初查只有管理端 NodeBridge PID 11672，SyncAgent 尚未启动。在已验证保存的连接正常后，使用安装版 SSH MCP 的 `nodebridge_start_agent` 启动，PID 1516。断开并重新建立 SSH/MCP 后仍为同一 PID，Canal/Agent 状态为 running。

中心 `192.168.10.103` 保持原 v0.46.6 测试 Agent，路径 `.cache/fb054/original/SyncAgent.exe`、PID 14048。本轮没有擅自升级中心或改变两端配置/规则。

## 连接探测

| 检查 | 结果 |
| --- | --- |
| SSH 公钥连接 | 通过 |
| 安装版 MCP initialize / tools/list | 通过，32 个工具 |
| `nodebridge_test_mysql` | `ok=true`，mysql reachable |
| `nodebridge_test_rabbitmq` | `ok=true`，local/server rabbitmq reachable |
| `nodebridge_agent_status` | 安装路径、PID 1516、running |
| `nodebridge_overview` | MySQL/RabbitMQ/CDC/Agent 均 running |
| `nodebridge_queue_status` | Edge upload/retry/downlink/dead 均 0 |
| `nodebridge_failed_events` | 空列表 |
| 中心入口队列预览 | 空 |

队列消费者数可能为 0，因为现有 Worker 使用轮询获取；不单靠消费者计数判断是否运行，以下真实数据探针作为链路证据。

## 真实数据

执行现有 `scripts/lab-real-latency-e2e.ps1 -Count 20`，使用原 `e2e-probe-edge-to-server` / `append_only` 规则，在 `scada_edge.nb_e2e_probe` 写入独立主键，等待 `scada_center.nb_e2e_probe` 可见；每次之间空闲 1200ms。

| 项目 | 结果 |
| --- | ---: |
| 成功探针 | 20 / 20 |
| 最小延迟 | 63.209 ms |
| P50 | 94.219 ms |
| P95 | 126.016 ms |
| 最大延迟 | 151.366 ms |
| 平均延迟 | 93.756 ms |
| 源/目标行数 | 20 / 20 |
| apply log / 唯一 apply event | 20 / 20 |
| SUCCESS event log | 20 |
| 本次事件关联错误 / FAILED ACK | 0 / 0 |

所有主键、probe_value 和 created_at 逐行精确一致。延迟包含控制端源写入往返及目标轮询，不是单纯网络 RTT。本次测试行保留为证据，没有清空表或队列。

## 边界与待办

当前正式规则只允许 Edge -> Server 追加写。本轮未增加 UPDATE、双向规则、50 列宽表或启动七小时长测，FB-055 继续待办。后续双端 v0.46.7 长测前还需更新中心运行版本。

发现非阻断版本文字问题：MCP initialize.serverInfo.version 与 overview.version 仍为 `0.46.6`。源码 `internal/mcpstdio/server.go` 和 `internal/datasyncui/app.go` 仍硬编码旧版本。实际安装包/hash/注册版本均确认是 v0.46.7；不是安装失败，登记 FB-057 给 backend-ai，本轮不改产品行为。

本轮通过 `go test ./...`、`go vet ./...`、`golangci-lint run ./...`（0 issues）。未执行长测、故障注入或 UI 交互验收。

证据：`.cache/fb056/mcp-start.json`、`mcp-check.json`、`latency.json`、`exact-consistency.json`。检查脚本仅存于该测试证据目录，现场规则 hash 始终保持 `CD26BB056B729B5D0A3614784467C849354E9A37181BF96ABE10FD6E8EFBE2CD`。
