# 0.48.5 独立七小时长测

负责人：test-ai。用户要求先让长测稳定运行，再结束 goal；七小时最终通过必须等待终态证据。

## 隔离范围

- 同一 Windows 主机上三个 0.48.5 SyncAgent，分别连接独占的三套 MySQL、三套 Canal，以及独占 RabbitMQ 的三个 vhost。
- 不改业务 AI 的规则、配置、业务表、对齐证明、Canal 游标或组件；只使用 C:/D:。本机原配置与规则运行前后核验 SHA256。
- 测试使用新建异名库表和主键、列映射，普通业务表不含同步专用标记。不是三台物理 Windows 机器覆盖。
- 测试 SQL 会话使用明确递增的源时间，避免此前系统时钟回退干扰可重复的 LWW 预期；不修改系统时间，不声称解决时钟偏差问题。
- 候选 SHA256：BD3ECC8ACC1C217484D2BD7E5C001FE9615A749F79196C091196C6B727DD580A。

## 覆盖与判据

| 阶段 | 内容 | 判据 |
| --- | --- | --- |
| 预启动 | 普通 MCP 列表、首次对齐启动/状态/中断、三节点 ACTIVE、提交后重试、规则 CAS 启停 | 完整组证明、基线行数及摘要一致 |
| 全量间隙 | 首成员完成后发生更新、删除 | 后加入成员保留获胜版本及墓碑 |
| 故障短测 | 两轮离线较新更新和删除、Agent 重启、回执表注入失败 | 全节点收敛，事务回滚后无业务行外泄，重试恢复 |
| 持续七小时 | 三来源各12行新增，另一个节点更新、第三个节点硬删；清理上一批 | 每批四阶段全表排序 SHA256 相同，行数符合独立预期 |
| 数据精度 | DECIMAL(30,10)、高位二进制、中文、NULL；显式事务回滚 | 新增值逐行检查，回滚行不出现，映射无损 |
| 计划离线 | 第2小时暂停Edge2，第4小时暂停Server，分别10分钟；离线期三端仍提交本批写入 | 重新启动后90秒内逐行/摘要收敛 |
| 结束门禁 | 修复队列为空、静止收据数稳定、不完整拓扑启动拒绝 | 测试 PASS、evidence.json passed=true、supervisor passed、原文件哈希不变 |

持续阶段每批结束间隔5秒，保留24行当批数据加既有功能样本，系统日志/版本记录持续积累。这不是最大吞吐量或200MiB全量容量测试。计划离线的10分钟期间本批写入已提交，等待重连，不承诺离线期间持续满负载。

## 本轮记录

首轮批量16失败：`.cache/multi-node-sync/052ecc915e0e4b90a5817ce62c2b5199`。
22:04:31开始持续阶段，首批新增等待90秒后三节点分别41/42/40行，未收敛。日志出现1213死锁及恢复，但缺行根因未确证。三库 `early-mismatch-*.sql`、`failure-state.json` 和日志已保留；不把降批量视作修复。该轮 supervisor 已 failed，独占容器退出，本机原文件哈希不变。

第二轮按用户允许跳过问题功能继续测试，改为批量1，保留三节点与其余全部场景：

- supervisor：`.cache/owned-soak/20260914-220617/supervisor.json`，PID3032。
- fixture：`.cache/multi-node-sync/dcee70b6467447519b6ee636420eefee`。
- 持续进度：fixture 下 `soak-progress.json`；实际开始/结束时间以此为准。
- 运行日志：`test-output.txt`；后台汇总：supervisor 同目录 `output.log`。
- 生命周期：后台 PowerShell 请求防休眠，测试期限为25200秒加准备/收尾预算；终态自动停止独占 Agent、保存日志并移除本轮容器/网络，保留本地证据。
- 如需停止：在 fixture 下创建 `stop.requested`，下一批入口检查并正常进入测试清理。计划离线期间最长需等本段恢复/检查；不要停止业务进程。

第二轮已在22:09完成10批、1056次CRUD，三端摘要相同；运行开始22:07:32，预计2026-09-15 05:07:32结束写入。supervisor及三个run Agent、七容器实时核验存活，原文件哈希不变；startup-observation-1.json已留存。Go test ./...、go vet ./...、golangci-lint run ./...和PowerShell解析通过；日志在.cache/v0485-release/soak-*.log。heartbeat自动化nodebridge每10分钟静默复查，无异常不通知。七小时终态和两次计划离线恢复已于2026-09-15 05:17验收，见下方终态记录。FB-094/107缺行与历史问题仍open，FB-084/109不属于本轮关闭范围。

稳定启动最终核验：startup-verified.json，进度由552次增至超过1056次，主管/三个Agent/七容器存活、最近摘要新鲜、原配置哈希相同。用户要求的稳定启动后结束goal条件已满足；长测继续后台运行，不能把此交接当作七小时通过。

## 终态验收（2026-09-15 05:17，test-ai）

第二轮批量1通过：持续阶段从2026-09-14 22:07:32.787至2026-09-15 05:07:38.008，实际7小时5秒；741批、80,004次CRUD。每批各阶段三端摘要一致，最后三端各30行，SHA256均为499fca32f3701709ac405b4b4a60f31a18b4673a4d151693bc33b262324964bc。

- Edge2计划离线：第262批在2h0m28s验证，第263批在2h10m59s恢复验证通过。
- Server计划离线：第458批在4h0m28s验证，第459批在4h10m50s恢复验证通过。两次各暂停10分钟，随后批次继续一致，faults_completed=2。
- test-output.txt明确PASS，TestOwnedMultiNodePipeline耗时25240.54秒；结束的修复队列、静止回执及不完整拓扑拒绝启动门禁通过。末尾沿用的“Short functional test, not a soak test”是旧日志文案，本轮时长依据soak-progress.json及741条持续阶段日志。
- evidence.json passed=true，候选哈希匹配；supervisor status=passed，05:07:50完成，originals_unchanged=true。05:17再次计算本机配置/规则哈希与启动值一致。
- 实时核验主管及本轮所有Agent均已退出，七个独占容器和本轮网络已清理；本地日志、进度、证据保留。
- 首轮052ecc915e0e4b90a5817ce62c2b5199三库SQL仍保留。批量16缺行问题未修复，FB-094/107仍open；FB-084/109不变。本轮仅关闭批量1七小时长测验收，不代表三台物理Windows、真实时钟偏差或最大吞吐量通过。

本次仅更新验收文档，产品与测试代码未改动；沿用启动前已通过的Go test/vet/lint。终态处理后撤销本轮复查自动化。
