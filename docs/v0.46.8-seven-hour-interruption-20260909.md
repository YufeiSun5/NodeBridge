# 七小时测试中断检查

Owner: test-ai；2026-09-09 16:40 Asia/Singapore。Board: FB-055 blocked，FB-066 open。

后续诊断、v0.46.9候选包及正式安装要求见`docs/v0.46.9-diagnostics-and-upgrade-20260909.md`；本文件保留最初中断检查，不将后续修复追认为本轮通过。

## 结论

正式run `wide7h_0909_161239`失败，未完成七小时。16:13:22开始，16:34:10因`wide command timeout: snapshot`触发停产，16:34:26自动恢复结束，总计约21分钟。已验证两端临时Agent的最终包hash，但未完成双端正式安装后的完整软件验收；目前没有证据将超时归因于未安装，也不能据此排除产品问题。

直接原因：监督器将一次同步状态采样限制为15秒，超时后按失败门禁停止测试。故障前10次实际快照查询耗时约5.59至11.02秒。最后一次`command-150-snapshot.out/.err`为空，未记录逐条SQL耗时，因此尚不能确定是哪条SQL或连接阶段耗尽预算。<!-- 待确认 -->

## 已核查

- 每端生产账本1249 tick，24980 INSERT、12490 UPDATE，随后收到监督器stop requested；不是生产器自行停止。
- 两方向流水源表与目标表当前均24980行；25032次在线版本观察未报告回退。未执行最终50列/事务oracle全量验证，不能用行数相等宣称没有数据问题。
- 最后成功快照中失败事件和失败ACK均0；4个CRUD生命周期完成，尚未到正式故障注入/DDL/峰值阶段。
- 中心ingress/downlink/dead当前ready/unacked均0；历史隔离队列仍642条，未purge。
- `restoration.json` passed：Edge恢复安装路径PID7872，Server恢复原备份路径PID17896，原规则hash一致，配置核验通过。临时唤醒PID10724于16:34:27因规则恢复释放。
- 只读EXPLAIN显示Server失败事件统计按`idx_table_pk`扫描本run约77072条事件，再过滤status；停载后实测约491ms。该查询无(table_name,status)覆盖索引，是需评估的增长成本，不能仅据此认定为本次唯一根因。
- Edge的FAILED ACK统计当前表为空，实测约0.022ms，不支持将ACK表扫描认定为当前瓶颈。
- MySQL统计存在异常巨大计时值，未将其作为可靠耗时依据；优先采用实际快照文件时间和EXPLAIN实测。

## 后续边界

用户已明确愿意正式安装更新，不以避免升级作为约束。先定位问题；需要产品修复则由backend-ai修复并出包，test-ai核验双端正式安装路径、版本、hash及正常启动入口，再做准入和从零开始的七小时测试。临时二进制仅用于诊断，不代替安装验收；不降低负载、删失败样本或跳过检查，不将旧失败追认为通过。

本次未修改产品代码、数据库索引、运行配置或安装目录，未重启新一轮长测。已保留原准入证明到run目录`formal-ready-at-start.json`，将当前`formal-ready.json`失效并实测正式Start被拒绝。

FB-066下一步：为每条快照SQL/连接补独立耗时和上下文期限，评估计数/日志查询的索引与增量方案，并将昂贵采样与监督心跳解耦；保留真实错误门禁，不能只提高超时数值掩盖持续退化。先做超过本次21分钟且含历史日志压力的准入复测，再考虑重开七小时。

证据目录：`.cache/wide-seven-hour/wide7h_0909_161239/`，包括state、events、producer、command-snapshot、version-observations和restoration。
