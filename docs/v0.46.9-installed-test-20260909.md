# v0.46.9 实装功能与持续负载验收

身份：test-ai。记录时间：2026-09-09 18:23 Asia/Singapore。

## 当前结论

双端正式安装、原配置保留、MCP、中心标准迁移及前置同步功能检查通过。
30分钟短测 `widesmoke_0909_180024` 于18:17:07判定失败，18:17:22恢复结束，不能视为完整短测或七小时通过。
正式长测准入仍关闭，必须完成数据、账本、延迟、场景和恢复门禁后才可签发。

## 实装证据

- 两端注册版本 `0.46.9`，均运行 `C:\Program Files\NodeBridge\app\SyncAgent.exe`。
- 包 SHA256：`6C26E6D397C36238A7DEDCBB246D0EC39368F23C40D741F04917B9F6E699514F`。
- Agent SHA256：`9DF8C21E8959E98FE957AF58F31434F6E1BDD847FC6AA7266221795E11C50CDD`。
- Edge 用户安装摘要：`20260909-175003-401-1b844d4d`；Server 正式升级摘要：`20260909-175452-022-d34efbf7`，均成功。
- Server 原备份目录进程先按路径、命令行和 stop-file 优雅停止，正式升级后通过安装版 MCP 启动；不再临时运行候选二进制。
- Server 配置仅 DPAPI 密文重新生成，解密后的完整文本逐字一致，规则 hash 不变；Edge 当前配置 hash 与安装后初始复核一致。
- 双端运行实例、登记、路径和 hash 证据：`.cache/wide-seven-hour/widesmoke_0909_180024/manifest.json`。
- 安装/配置/MCP/迁移证据：`.cache/v0469-installed-20260909-1754/`。其中配置备份含敏感值，不用于对外报告。

## 功能结果

- Edge MCP initialize 为 `0.46.9`，33 个工具；两端安装版 MCP 启动 Agent 成功，SSH 会话关闭后 Edge 保持运行。
- 两端 MySQL 诊断成功；Server 标准 `migrate -scope server` 成功。
- 原故障 run 的77072条事件仍保留，失败事件0；失败计数1.311ms，总计数80.994ms，全诊断104.852ms。
- EXPLAIN 使用 `idx_table_status`，`using_index=true`。该结果不证明历史15秒超时的唯一根因。
- 短测前置50列映射校验、同批重复事件、旧 UPDATE 重放、同键所有权交接通过。
- `go test ./...`、`go vet ./...`、`golangci-lint run ./...`（0 issues）和监督器回归通过。

## 夹具修复 FB-069

NSIS 实际在32位注册表视图登记，旧测试只查64位导致误报未安装。
测试现在同时读取两种视图，拒绝冲突根目录/版本及不完整登记。
32位、64位、相同双登记、冲突、空登记回归及双端实装预检通过。
未改产品行为，未重打安装包，路径/版本/hash/进程门禁未放宽。

## 失败的短测

- 负载开始：18:01:12；计划结束：18:31:12；持续时间1800秒。
- 原15秒 snapshot 门禁及14秒共享SQL期限保留，每一步trace留在 `command-*-snapshot.err`。
- 保持固定负载、双向真实UPDATE、生命周期、双端停机恢复、在线DDL、晚期精度与重放探针和全50列最终校验。
- Edge 临时唤醒 PID14952，最大到18:42:20，规则恢复后提前释放；不改电源方案，不防手动睡眠/关机。
- 每端35660 INSERT/17830 UPDATE；边缘停机68.482秒、中心停机72.885秒，两次积压均在限时内恢复。
- `metrics-167.err`记录中心MySQL读取30秒超时；最终50列、精确账本、全部DDL/晚期门禁未完成。
- MySQL performance_schema中Apply分组统计同形查询MAX31.452秒，EXPLAIN为ALL约802005行且Using temporary；尚无原失败逐步trace，不宣称单次SQL唯一根因已完全证明。
- `restoration.json` passed：原配置、规则、安装版路径恢复。临时唤醒18:17:25因规则恢复释放。
- 随后受控停双端，对本run消息逐条核对table/event_id并publisher-confirm后ACK转入持久队列`nb7_widesmoke_0909_180024.quarantine`，共3189条；证据`quarantine.json`。不清空或删除消息，两端随后恢复启动。
- 尚未启动新七小时测试。

## 看板

FB-069 closed；FB-068 blocked；FB-070 running；FB-066 open；FB-055 blocked。历史失败不追认通过。
