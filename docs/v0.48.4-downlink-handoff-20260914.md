# 0.48.4 双向目标业务库与 Canal 过滤修复

## 交付文件

| 文件 | 大小 | SHA256 |
| --- | ---: | --- |
| `build/NodeBridge-beta-v0.48.4-20260914.exe` | 340900188 字节 | `DC66A3E75D6DA5F71F8836752B5D1B8880D867220F4FA3C639BFF786D5B2B4F0` |
| `build/NodeBridge-agent-v0.48.4-20260914.zip` | 9167318 字节 | `173F187ECC756C5329960D5254466E034BC702D15C3D94426D959CCAA3F2E041` |
| 包内 `SyncAgent.exe` | — | `D5C3F5CF37FF1C0DA43187EE09D309CA9D088B952101F00FCCB6579EC6610251` |

安装包解包核对 62 文件、20 个系统迁移和 5 个依赖资产通过；便携 ZIP 的 Agent 和 20 个迁移逐项哈希通过。安装包未签名。证据 `.cache/v0484-release/package-gates.json` 与 `portable-gate.json`。

## 修复范围

用户提供的业务复测报告确认：从站到主站 CRUD 已通过；主站到从站的事件已到达，但目标库被覆盖为 `scada_edge`，导致 `target_schema_unavailable`。本轮核对源码后修复程序逻辑，未修改现场规则、业务程序、业务表或失败消息。

`CompileEndpoint` 已正确反转双向映射。错误发生在下行单条/批量 Apply 前：`DownlinkTargetDatabase` 只让 `SERVER_TO_EDGE` 的显式目标库优先，`BIDIRECTIONAL` 却使用 `cfg.MySQL.Database`。现在双向反向和多边缘中继同样保留编译后的目标业务库；不再次反转规则。原单向上传中继的本机默认库回退语义保留。失败诊断也使用相同选择逻辑。

另修复 `conflictCaptureFilter` 对逗号列表整体加括号的问题。Canal 会按逗号拆分表达式，现在以独立表达式追加系统库的 `sync_capture_fence` 和 `sync_replay_marker`，保留原业务过滤；已改成竖线组合的有效配置同样兼容。

本版包含 0.48.3 小屏规则界面。没有新增 API、DTO、规则字段或数据库迁移；不要求业务新增字段或表。

## 验证与边界

- 目标库选择覆盖双向显式目标、缺省回退、单向下发以及原上传中继兼容。
- 单条和批量下行均覆盖 INSERT/UPDATE/DELETE、不同源目标库表与主键映射；失败不 ACK、重新入队，重试保留原事件身份及正确目标库后成功 ACK。
- 三节点规则图覆盖主站到两边缘及边缘间中继，显式系统默认库不能覆盖异名业务库。
- Canal 多表达式可分别编译，保留业务匹配并只增加本机系统捕获/回放表。
- `go test ./...`、`go vet ./...`、`golangci-lint run ./...`、Wails/前端构建、绑定检查、CLI smoke 与 MCP 版本/42 工具检查通过。证据 `.cache/v0484-release/`。
- 本轮新增验证为单元与运行时模拟回归；需要外部端点的集成测试按其环境条件跳过。没有进行真实 MySQL/RabbitMQ/Canal 链路复验，也没有安装到业务两端。因此不能将业务现场主站到从站 CRUD 标为通过。

## 现场接续

原报告：`D:/DEV_D/spindle/V2_1/edge-terminal/main-server/docs/sync-configuration-20260914/v0482/recheck/README.md`。以下为该报告记录，操作前应重新核对现场状态。

1. 停止同步并显式退出旧 UI 后升级参与节点，保留各自连接、规则、首次对齐账本和 Windows 账户。便携 Agent 需更新 MCP 实际路径并重开 stdio 会话；旧进程不会自动变成新版。
2. 已完成 0.48.1/0.48.2 的系统迁移 010 时，本次不新增迁移。系统连接库仍为 NodeBridge 系统库；不要改成业务库绕过错误。
3. 沿原业务授权恢复相应规则。保留并继续处理主站 `edge-001.downlink.q` 中事件 `8ed2238853c14ae6c3a44cfe85fe5a1913a1655be0d9ec097529cca45f257a12`（测试主键 `89000000001033`）及随后待捕获的清理 DELETE，不跳过、手动 ACK 或清空队列。
4. 检查实际目标为 `spindle_edge_ac01.sys_detection_standards`，原事件与后续 DELETE 顺序完成，两端都无该测试行，再复验主站到从站的完整 CRUD 和双向收敛。核对原业务数据未变。

owner `backend-ai`，跨 `test-ai` 仅回归及发布检查。本轮代码修复完成；现场复验 pending，FB-094/107 历史问题、FB-084 原生界面验收及 FB-109 依赖风险保持原状态。
