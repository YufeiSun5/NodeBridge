# .ai/agents

本目录存放自定义 Agent 定义。

## 命名规范

- 文件名使用小写短横线：`architecture-review.agent.md`。
- `tools` 使用最小必要能力声明，例如只读审查使用 `[read, search]`。

## 创建条件

- 任务边界清晰。
- 可以被多个编辑器或 Agent 复用。
- 有稳定的检查清单或工作步骤。

默认不创建深度审阅流程产物；仅当用户明确要求“启用审阅流程”、“加上深阅”或 `/review` 时再增量创建。
