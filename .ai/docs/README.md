# .ai/docs

本目录存放功能设计、架构摘要、变更归档和长期跟踪文档。

## 命名规范

- 使用小写短横线：`product-design.md`、`rabbitmq-topology.md`。
- 已失效或历史版本移动到 `archive/`。
- `changelog.md` 用于归档过长的 `MEMORY.md` 历史记录。

## 创建条件

- 某个主题需要超过 `AGENTS.md` 可承载的长度。
- 需要记录架构决策、模块边界、协议、部署、迁移或诊断流程。
- 需要保存已经确认的产品设计摘要，供多个 Agent 复用。
